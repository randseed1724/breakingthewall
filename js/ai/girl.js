Layout.prototype.girl = function() {

      var runFromLeft = 1 ;
      var runFromRight = 73 ;

      var start = $("#grid"+ runFromRight);

      start.addClass( "girlFromLeft" );  //Inicial Position
      var move = $("#grid"+ direction);

      var keyPressed = false;

      // $("#grid73").

};
